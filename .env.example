# Copy this file to `.env` and set the values appropriately.
# These environment variables configure the backend and optional services.

# API settings
API_HOST=0.0.0.0
API_PORT=8000

# GEMINI API configuration
GEMINI_API_KEY=your_google_genai_api_key_here

# Database connection URL (optional if using a database)
DB_URL=sqlite:///./inventory.db

# Other custom settings can be added here